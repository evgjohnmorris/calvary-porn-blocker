{
  "name": "porn-blocker-workspace",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "clean": "pnpm -r clean"
  },
  "pnpm": {
    "overrides": {
      "rollup": ">=2.80.0",
      "esbuild": ">=0.25.0",
      "vite": ">=6.4.2"
    }
  }
}